import { Platform, Dimensions } from 'react-native';

export const APP_NAME = 'Mandrake';
export const VERSION = '1.00';

export const { OS } = Platform.OS;
export const Viewport = Dimensions.get('window');
